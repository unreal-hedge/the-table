# Where the browser finds the game server (party/).
# Local dev: `npm run party:dev` serves on 127.0.0.1:8787 (leave unset).
# Production (set in Vercel): the-table.<your-subdomain>.workers.dev
NEXT_PUBLIC_PARTY_HOST=127.0.0.1:8787
